numpy>=1.24
matplotlib>=3.7

# tkinter is NOT installed via pip - it ships with standard Python.
# If "import tkinter" fails on Linux, install it with your system
# package manager, e.g.: sudo apt install python3-tk
